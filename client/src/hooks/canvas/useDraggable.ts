// Simplified placeholder since we used framer-motion drag
export const useDraggable = () => {
    // Not strictly needed with framer-motion drag prop, but keeping file structure consistent
    return {};
};
